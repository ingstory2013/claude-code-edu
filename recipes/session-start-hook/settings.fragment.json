{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git rev-parse --is-inside-work-tree >/dev/null 2>&1 || exit 0; echo \"현재 브랜치: $(git branch --show-current)\"; echo \"변경된 파일: $(git status --porcelain | wc -l | tr -d ' ')개\"; echo '최근 커밋:'; git log --oneline -3"
          }
        ]
      }
    ]
  }
}
